#!/bin/bash

. "$HOME/.local/bin/common_script.sh"

if ! command_exists ufw; then
  case "$PACKAGER" in
  pacman)
    "$AUR_HELPER" -S --noconfirm ufw
    ;;
  *)
    "$ESCALATION_TOOL" "$PACKAGER" install -y ufw
    ;;
  esac
fi
